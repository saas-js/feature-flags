import store from "./store";
export type {
  AttrMap,
  Feature,
  Flags,
  Segment,
  UserAttributes,
  FeaturesOptions,
  FeaturesStore,
  FeaturesStoreValue,
} from "./types";

export default store;
